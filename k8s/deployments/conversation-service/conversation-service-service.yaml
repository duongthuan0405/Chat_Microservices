apiVersion: v1
kind: Service
metadata:
  name: conversation-service
  namespace: default
  labels:
    app: conversation-service
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
  selector:
    app: conversation-service