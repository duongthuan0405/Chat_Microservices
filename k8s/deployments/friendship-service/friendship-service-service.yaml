apiVersion: v1
kind: Service
metadata:
  name: friendship-service
  namespace: default
  labels:
    app: friendship-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
  selector:
    app: friendship-service